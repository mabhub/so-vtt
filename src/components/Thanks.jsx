import { Box, Typography } from '@mui/material';

const Thanks = () => (
  <Box>
    <Typography variant="h1">
      Merci !
    </Typography>

    <Typography variant="body1">
      Cet évènement sera très bientôt visible dans la liste.
    </Typography>
  </Box>
);

export default Thanks;
